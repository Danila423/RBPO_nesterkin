# P10 — SAST & Secrets (Semgrep + Gitleaks)

| Item | Details |
| ---- | ------- |
| Workflow | `.github/workflows/ci-sast-secrets.yml` (docker Semgrep `p/ci` + кастомные правила, Gitleaks с allowlist) |
| Evidence | `EVIDENCE/P10/semgrep.sarif`, `EVIDENCE/P10/gitleaks.json` (краткий итог фиксируется в P10-чеклисте PR) |
| Customization | Добавлены правила Semgrep для FastAPI (поиск захардкоженных JWT и DSN) и настроен allowlist Gitleaks под dev-плейсхолдеры |
| Triage status | 2 Semgrep + 2 Gitleaks срабатывания признаны dev-конфигом; план миграции секретов фиксирован в SEC-15 |
| Usage plan | Workflow запускается на push/PR, поддерживает baseline и готов выдавать SARIF в Code Scanning, когда включим интеграцию |
